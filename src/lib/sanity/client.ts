// Public Sanity config — no auth token.
// Used for image URL building only. All data queries
// MUST use the authenticated server client ($lib/server/sanity.ts).

export const projectId = '0mg3gtri';
export const dataset = 'production';
export const apiVersion = '2025-10-01';
